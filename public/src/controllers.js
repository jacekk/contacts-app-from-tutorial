// Generated by CoffeeScript 1.8.0
(function() {
  angular.module('ContactsApp').controller('ListController', function($scope, $rootScope, Contact, $location) {
    $rootScope.PAGE = 'all';
    $scope.contacts = Contact.query();
    $scope.fields = ['firstName', 'lastName'];
    $scope.sort = function(field) {
      $scope.sort.field = field;
      $scope.sort.order = !$scope.sort.order;
    };
    $scope.sort.field = 'firstName';
    $scope.sort.order = false;
    $scope.show = function(id) {
      $location.url("/contact/" + id);
    };
  }).controller('NewController', function($scope, $rootScope, Contact, $location, $timeout) {
    $rootScope.PAGE = 'new';
    $scope.contact = new Contact({
      firstName: ['', 'text'],
      lastName: ['', 'text'],
      email: ['', 'email'],
      homePhone: ['', 'tel'],
      cellPhone: ['', 'tel'],
      birthday: ['', 'date'],
      website: ['', 'url'],
      address: ['', 'text']
    });
    $scope.save = function() {
      if ($scope.newContact.$invalid) {
        $scope.$broadcast('record:invalid');
      } else {
        $scope.contact.$save({
          responseType: 'json'
        });
        $timeout(function() {
          $location.url('/contacts');
        }, 100);
      }
    };
  }).controller('SingleController', function($scope, $rootScope, Contact, $location, $routeParams) {
    $rootScope.PAGE = 'single';
    $scope.contact = Contact.get({
      id: $routeParams.id
    });
    $scope.remove = function() {
      $scope.contact.$delete().then(function(data) {
        $location.url('/contacts');
      });
    };
  });

}).call(this);
